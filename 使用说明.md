# xhs-video-skills · 使用说明（自用）

把「做一条小红书带货视频」这件事封装成一个 Claude Code skill：
环境体检 → 脚本分镜 → 剪辑 / 动效 → 字幕 → 封面 → 规格化导出 → 合规检查。

## 目录

```
xhs-video-skills/
├── SKILL.md                 主流程（Claude 读这个）
├── 使用说明.md               本文件
├── references/              规格、钩子库、流水线命令、ffmpeg 速查、品牌与合规、发布清单
├── scripts/                 零第三方依赖的 Python 脚本（只要 ffmpeg）+ setup.sh + selftest.py
├── templates/
│   ├── 脚本模板.md           分镜表
│   ├── DESIGN.md            品牌设计规范（给 HyperFrames 用）
│   └── hyperframes-vertical/ 1080×1920 五幕带货模板，复制即渲染
└── 示例/对话示例.md          一次完整对话
```

## 启用

- **本项目内**：已建好符号链接 `.claude/skills/xhs-video-skills → ../../xhs-video-skills`，
  在 `xhs-goods` 目录里启动 Claude Code 即可用。
- **全局**：`ln -sfn "$(pwd)/xhs-video-skills" ~/.claude/skills/xhs-video-skills`
- 改完 SKILL.md 或 references 之后，新开一个会话才会重新加载。

## 快速开始

```bash
python3 xhs-video-skills/scripts/selftest.py      # 应看到 All 13 checks passed
python3 xhs-video-skills/scripts/doctor.py        # 看哪几条流水线可走
```

然后对 Claude 说，例如：

> 用 xhs-video-skills 给托福写作批改考官做一条 30 秒带货视频，屏录在 ~/Desktop/toefl-demo/，钩子用「改版」。

它会先出分镜表等你确认，再动手。

## 四条流水线一句话

| | 适用 | 核心工具 |
|---|---|---|
| A1 | 有口播 / 屏录 + 有 ElevenLabs key | video-use 自动剪 |
| A2 | 有口播 / 屏录，无 key | 本地 `silence_cut → to_vertical → burn_subs → xhs_export` |
| B | 零素材，只有文字和截图 | HyperFrames 竖屏模板 |
| C | 片头 / 价格卡 + 真实演示 | B + A，`concat.py` 拼 |
| D | 要真人 / 场景镜头 | AI 生成模型（付费） |

## 常见问题

- **video-use 报没有 key**：在 `~/Developer/video-use/.env` 写 `ELEVENLABS_API_KEY=...`；没有 key 就走 A2。
- **HyperFrames 第一次很慢**：`npx` 要下载 CLI 和一份 Chrome，只有第一次。
- **字幕变方块**：`burn_subs.py` 会把系统中文字体目录传给 libass；如仍异常，加 `--font "Hiragino Sans GB"` 或 `--font-file /System/Library/Fonts/Supplemental/Songti.ttc`。
- **已有的 720×1280 视频**：`xhs_export.py` 会放大到 1080×1920，但清晰度不如重录；屏录时按 `references/04` 的尺寸录。
- **HyperFrames skill 版本**：2026-09-05 已升级为模块化版本（hyperframes-core 等 9 个），旧的 house-style / palettes 文件不再存在，模板已按新规范写。
